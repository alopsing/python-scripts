# lambda function to autoscale based on app names in dynamo db

### commands

- deploy everything
```bash
serverless deploy -v
```
- deploy just the function
```bash
serverless deploy function -f hello
```
- invoke the function
```bash
serverless invoke -f hello -l
```
